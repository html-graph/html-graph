import { Canvas } from "@/canvas";

export class RectangularSelectionConfigurator {
  public static configure(canvas: Canvas, overlayLayer: HTMLElement): void {
    console.log(canvas, overlayLayer);
  }
}
